name: 'f1_marts'
version: '1.0.0'
config-version: 2

profile: 'f1_marts'

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  f1_marts:
    staging:
      +materialized: view
    marts:
      +materialized: table
